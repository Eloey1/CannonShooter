using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Script : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //"Nice script! Men spelet är bajs"
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
